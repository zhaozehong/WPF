<local:KeyboardControl x:Class="WPF.UserControls.SmallKeyboardControl" x:Name="KeyboardContainer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF.UserControls">
  <UserControl.DataContext>
    <local:SmallKeyboardControlViewModel/>
  </UserControl.DataContext>
  <UserControl.Resources>
    <DataTemplate DataType="{x:Type local:NumberKeyboardControlViewModel}">
      <local:NumberKeyboardControl Switched="KeyboardControl_Switched" Closed="KeyboardControl_Closed"
                                   ButtonSize="{Binding ElementName=KeyboardContainer, Path=ButtonSize}"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type local:CalculatorKeyboardControlViewModel}">
      <local:CalculatorKeyboardControl Switched="KeyboardControl_Switched" Closed="KeyboardControl_Closed"
                                       ButtonSize="{Binding ElementName=KeyboardContainer, Path=ButtonSize}"/>
    </DataTemplate>
  </UserControl.Resources>
  
  <StackPanel>
    <ContentControl Content="{Binding CurrentViewModel}"/>
  </StackPanel>
</local:KeyboardControl>
